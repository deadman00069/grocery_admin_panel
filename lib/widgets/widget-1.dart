import 'package:flutter/material.dart';

class Widgett1 extends StatelessWidget {
  const Widgett1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
